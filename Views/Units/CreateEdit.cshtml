@model HangOut.Models.Inventory.INTUnits
@using HangOut.Models.Inventory;
@{
    Layout = null;
    var ObjOrg = Request.Cookies["UserInfo"];
}

<div class="card">
    <div class="card-header">
        <h4 class="text-danger text-center"><b>Units</b></h4>
        <hr />
    </div>
    <div class="card-body">
        @using (Ajax.BeginForm("CreateEdit", "Units", new { ID = Model.UnitID }, new AjaxOptions { HttpMethod = "POST", LoadingElementId = "waiting", OnSuccess = "OnSuccess" }))
        {
            <div class="row">
                @Html.HiddenFor(model => model.UnitID)
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="col-md-4">

                </div>
                <div class="col-md-4">
                    @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label  " })
                        @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                        @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                </div>
                <div class="col-md-1">
                    @Html.Label("Save", new { @class = "text-white" })
                    <input style="margin-top:25px" type="submit" value="Save" class="btn btn-success" />

                </div>
            </div>
        }
    </div>
</div>
