@model IEnumerable<HangOut.Models.HG_Orders>
@using HangOut.Models 
@{ Layout = null;}
@{List<vw_HG_UsersDetails > listItem = new vw_HG_UsersDetails().GetAll(); }
<div class="card">
    <div class="card-header">
        <h1 style="text-align:center"><b>USER Order</b> </h1>
        <hr />
    </div>
   
    <div class="card-body">
        <table class="table table-bordered table-hover w-100" id="TBLID">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.OID)
                    </th>
                    <th>
                        @Html.DisplayName( "User Name")
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Status)
                    </th>
                    
                    <th>
                        @Html.DisplayName("Order Date")
                    </th>
                    
                </tr>
                </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.OID)
                        </td>
                        <td>
                            @{vw_HG_UsersDetails objitem = listItem.Find(x => x.UserCode == item.CID); }
                            @objitem.UserName
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Status)
                        </td>
                        
                        <td>
                            @Html.DisplayFor(modelItem => item.Create_Date)
                        </td>
                         
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>