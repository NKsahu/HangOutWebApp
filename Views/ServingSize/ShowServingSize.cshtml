@model IEnumerable<HangOut.Models.HG_Items>
@{
    Layout = null;
}
<table class="table table-hover w-100" id="ServingItem">
    <thead>
        <tr>
            <th>
                Select
            </th>
            <th>
                @Html.DisplayName("Name")
            </th>
        </tr>
    </thead>
    <tbody id="Tbody">
        @foreach (var SSize in Model)
        {
            <tr id="@SSize.ItemID">
                <td>
                    <input type="checkbox" onclick="AddSSize(this,@SSize.ItemID)" class="form-control" />
                </td>
                <td>
                    @SSize.Items
                </td>
            </tr>
        }
    </tbody>
</table>
<script>
    setTimeout(function () { LoadDataTable('ServingItem') }, 200);
</script>
