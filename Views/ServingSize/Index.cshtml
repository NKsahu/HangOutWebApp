@model IEnumerable<HangOut.Models.POS.ServingSize>
    @{ 
        Layout = null;
    }
<div class="card">
    <div class="card-header">
        <h3 style="text-align:center"><b>SERVING SIZE</b> </h3>
        <hr />
        <button style="float:left;" onclick="BULKEXCELUPL()" class="btn btn-lg btn-info ">BULK UPLOAD</button>
        <button style="float:right !important;" onclick="DownloadExl()" class="btn btn-lg btn-info ">DOWNLOAD EXCEL</button>
    </div>
    <div class="row">
        <div class="col-md-3">
            <a href="/HG_Items/Index" data-ajax="true" data-ajax-update="#DynamicData" data-ajax-mode="replace" data-ajax-loading="#waiting" class="btn btn-default  blackTest form-control">ITEM</a>
        </div>
        <div class="col-md-3">
            <a href="/HG_Category/Index" data-ajax="true" data-ajax-update="#DynamicData" data-ajax-mode="replace" data-ajax-loading="#waiting" class="btn btn-default blackTest form-control">CATEGORY</a>
        </div>
        <div class="col-md-3">
            <a href="/HG_Category/AddonsIndex" data-ajax="true" data-ajax-update="#DynamicData" data-ajax-mode="replace" data-ajax-loading="#waiting" class="btn btn-default blackTest form-control">ADD ON</a>
        </div>
        <div class="col-md-3">
            <a href="/ServingSize/Index" data-ajax="true" data-ajax-update="#DynamicData" data-ajax-mode="replace" data-ajax-loading="#waiting" class="btn btn-default blackBackground blackTest form-control">Serving Size</a>
        </div>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-5"></div>
            <div class="col-md-2">
                <button onclick="CreateEdit('0')" class="btn btn-info form-control">Add One</button>
            </div>
            <div class="col-md-5"></div>
        </div>
        <table class="table table-hover w-100" id="TBLID">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.ServingId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    
                    <th class="noExport"> </th>
                </tr>
            </thead>
            <tbody id="Tbody">
                @foreach (var SSize in Model)
                {
                    <tr id="@SSize.ServingId">
                        <td>
                           @SSize.ServingId
                        </td>
                        <td>
                            @SSize.Name
                        </td>
                        <td>
                            <button class="btn btn-warning btn-sm " onclick="CreateEdit(@SSize.ServingId)">Edit</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<script>
   
    var AddTr = false;
    function JsonSuccess(Jobj) {
        if (Jobj.data != undefined && Jobj.data == '1') {
            $('#waiting').hide();
            alert('Uploaded');
        }
        else {
            hidedpt("CreateSS");
            $('#waiting').hide();
            var Jitmobj = Jobj.data;
            var HtmlStr = "<tr id='" + Jitmobj.ServingId + "'>";
            HtmlStr += '<td>' + Jitmobj.ServingId + '</td>';
            HtmlStr += '<td>' + Jitmobj.Name + '</td>';
            HtmlStr += '<td><button class="btn btn-warning btn-sm " onclick="CreateEdit(' + Jitmobj.ServingId + ')"> Edit</button>';
            var table = $('table').DataTable();
            if (AddTr) {
                //$("#Tbody").append(HtmlStr);
                table.row.add($(HtmlStr)).draw();
            }
            else {
                table.row("#" + Jitmobj.ServingId).remove().draw();
                table.row.add($(HtmlStr)).draw();
                //$("#Tbody").append(HtmlStr);
            }
        }
    }
    function CreateEdit(ID) {
        if (ID != "0") {
            AddTr = false;
        } else {
            AddTr = true;
        }
        $('#waiting').show();
        $.ajax({
            type: 'GET',
            url: "/ServingSize/CreatEdit?ID=" + ID,
            success: function (data) {
                makedpt("CreateSS",400,400);
                $("#CreateSS > .modal-dialog > .modal-content > .modal-body").html(data);
                showdpt("CreateSS");
                $('#waiting').hide();
            },
            error: function (jqXhr, textStatus, errorMessage) { // error callback
                $('#waiting').hide();
            }
        });

    }
    function BULKEXCELUPL() {
        $('#waiting').show();
        $.ajax({
            type: 'GET',
            url: "/HG_Items/UplExl",
            success: function (data) {
                makedpt("CreateSheat");
                $("#CreateSheat > .modal-dialog > .modal-content > .modal-body").html(data);
                showdpt("CreateSheat");
                $('#waiting').hide();
            },
            error: function (jqXhr, textStatus, errorMessage) { // error callback
                $('#waiting').hide();
            }
        });
    }
    
    function DownloadExl() {
        if (confirm('Are You Sure')) {
            $('.buttons-excel').click();
        }

    }
    
</script>

